# cloud-node-rust 1.0.8

本文件记录 `1.0.8` 的发布说明。

## 概述

1.0.8 重点修复 SNI passthrough、TCP 和 UDP 四层代理的统计与访问日志细节，并继续补齐 GoEdge/FlexCDN 兼容行为：修正访问日志、统计和节点状态中的时间偏移，按 EdgeNode 方式处理 ACME HTTP-01 challenge，修复动态 SSL 证书删除后旧证书仍被选择的问题，并新增单节点本地计费流量口径配置。默认继续保持 FlexCDN/GoEdge 的单下行计费口径；需要时可在本地配置中启用上下行合计计费。

## 统计和计费

- 修正 SNI passthrough 和 TCP 双向转发在异常断开时丢失已转发字节的问题，失败前的上下行流量现在会继续进入统计链路。
- 修正 UDP 代理统计：按包记录上下行增量，失败和会话结束路径都会正确调用请求结束逻辑，避免 active connection 泄漏。
- 确认 SNI、普通 L7、TCP 和 UDP 均复用控制面的 Server 级统计接口上报，不新增不兼容接口。
- 服务级带宽上报对齐 EdgeNode 的 `bandwidthAlgo`：用户配置为 `avg` 时按 5 分钟总流量平均带宽上报，其他情况继续按 2 秒采样峰值带宽上报。
- 新增 `billing.countInboundTraffic` 本地配置项，默认 `false` 时只把节点下行流量计入控制面带宽和日统计计费字段；设置为 `true` 后当前节点按上下行合计计费。

## SNI 访问日志和时间

- 修正 SNI passthrough 访问日志中 `request_uri` 使用完整 URL 导致控制面展示重复 URL 的问题。
- SNI 日志现在使用 `CONNECT host TCP` 请求行，并保持 `scheme=https`、`host` 和 `/` 路径分离。
- 补齐 SNI 访问日志的 `time_iso8601` 和 `time_local` 字段，避免控制面展示缺少时间。
- 修正访问日志 `timestamp` / `msec` 数值时间被本地时区偏移污染的问题，避免日志被写入未来时间导致控制面查询不到。
- 服务带宽、日统计、维度统计、Top IP 和节点在线状态改用系统真实时间上报，避免控制面时间校准 offset 再次叠加到业务统计时间。
- `cloud-node status` 增加当前版本和构建时间输出，便于确认线上二进制是否已替换到目标 Release。

## ACME 和 SSL 证书

- ACME HTTP-01 challenge 对齐 EdgeNode：节点收到 `/.well-known/acme-challenge/*` 后实时向 API 查询 token 对应 key，查到后由节点直接返回 `text/plain` challenge 内容；查不到或 RPC 失败时继续正常站点流程。
- ACME 处理提前到节点启停拦截之前，避免节点禁用状态误挡证书签发验证。
- 修复服务级 SSL 证书被错误保存为全局证书的问题；删除服务证书或移除服务后，证书选择器会按当前配置重建快照，不再继续使用旧证书处理请求。
- 单服务多个 SSL 证书按 SNI 选择：证书 SAN、CN 和控制面下发的 `dnsNames` 都会进入匹配表，支持同一服务绑定多个域名并按域名返回对应证书。
- SSLPolicy 开关按策略独立生效，某个服务关闭 SSLPolicy 不再导致其他服务或全局证书同步被跳过。

## 配置和文档

- `configs/api_node.yaml` 示例新增 `billing.countInboundTraffic`。
- 配置文档说明默认下行计费和可选上下行合计计费的差异。
- 安装脚本升级时默认获取最新 Release 并原子覆盖二进制，安装/升级完成后交互询问是否重启或启动服务。
- README 和运维文档更新到 1.0.8 发布入口。

## GitHub Actions 构建缓存

- Release workflow 现在会在 `main` 分支 push 时预热 release 构建缓存，并在 tag 发布时复用同一组缓存。
- main 分支预热只在 `.cargo/`、`pingora-main/`、`toa-main/`、`toa-sender/` 和 `vendor/` 这类基础框架或底层依赖变化时触发；普通业务代码、`build.rs`、`Cargo.toml`、`Cargo.lock`、proto 和测试变更不再触发预热。
- Rust release 构建缓存显式纳入 target、CPU variant 和编译 flags，避免不同 CPU 产物互相污染。
- 移除旧系统兼容包的自动构建和发布产物，官方 Release 仅保留现代 x64/ARM 产物。

## 验证

- 本轮本地验证执行 `cargo fmt -- --check`、`git diff --check` 和 `cargo check --locked --tests`。
- 未执行本地 `cargo build`，遵循只在本地执行 `cargo check` 的约束。
