# cloud-node-rust 1.2.5

## Cache correctness and invalidation

- Serialize cache-global regression tests so Fast L1, Bloom, negative-cache, and reclaim assertions cannot interfere when the test runner is parallel.
- Preserve the L2-to-Fast-L1 promotion path for small uncompressed objects and verify the returned `MemoryHitHandler` and representation length.
- Make Critical reclaim validation deterministic by seeding the negative-cache entry directly and checking actual L1 removal instead of relying on process-wide counters.
- Keep cache metadata hooks, purge fences, partial-cache publication, validator checks, and cache representation safety covered by the release verification matrix.

## Memory admission and toolchain

- Make control-plane admission tests use a deterministic memory snapshot, so the test validates rollback and rejection semantics independently of the host's available memory.
- Continue to build and lint with Rust 1.98.1 and strict `clippy -D warnings`; no warning suppression or cache-path bypass was added.

## Verification

- Rust 1.98.1 `cargo check --workspace --all-targets --locked`: passed.
- Rust 1.98.1 `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`: passed.
- Rust 1.98.1 `cargo test --workspace --locked`: passed.
- Project Rust files pass rustfmt checks. The repository-wide formatter remains affected by pre-existing trailing whitespace in `vendor/libinjectionrs`, which is external vendor code and was not modified.

## Upgrade notes

- No cache data format change.
- No cache admission, purge, partial-cache, or protocol behavior was intentionally downgraded.
