[package]
name = "cloud-node-xdp-common"
version = "0.1.0"
edition = "2024"
rust-version = "1.98.1"

[features]
default = ["std"]
std = []
aya = ["dep:aya", "std"]

[dependencies]

[target.'cfg(target_os = "linux")'.dependencies]
aya = { version = "0.14.0", optional = true }
